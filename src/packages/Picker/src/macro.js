const SHOWNUM = 7
const OFFSETY = 3
const SHOWNUM_HALF = (SHOWNUM - 1) / 2
const MAX_STEP = 800
const MIN_STEP = -800
export default {
  SHOWNUM, // 展示7列
  OFFSETY, // 拖拽差默认最小参数
  SHOWNUM_HALF, // 展示中间
  MAX_STEP, // 正最大步数
  MIN_STEP // 负最大步数
}
